{% include 'default/_form.html.twig' with {
    page_title: 'title.performer_edit',
    page_title_param: {'%id%': performer.id},
    attr: {
        method: 'PUT',
        action: url('performer_edit', {'id': performer.id}),
    },
    back_to_list_path: 'performer_show',
}
%}