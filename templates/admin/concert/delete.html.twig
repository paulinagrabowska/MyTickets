{% include 'default/_form.html.twig' with {
    page_title: 'title.concert_delete',
    page_title_param: {'%id%': concert.id},
    attr: {
        method: 'DELETE',
        action: url('concert_delete', {'id': concert.id}),
    },
    back_to_list_path: 'concert_show',
}
%}