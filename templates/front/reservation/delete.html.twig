{% include 'default/_front_form.html.twig' with {
    page_title: 'title.reservation_delete',
    page_title_param: {'%id%': reservation.id},
    attr: {
        method: 'DELETE',
        action: url('user_reservation_delete', {'id': reservation.id}),
    },
    back_to_list_path: 'main_page',
}
%}